---
title: Reserve
description: Reserve account for a single lending asset in Kamino
---

The `Reserve` account represents a lending pool for a specific asset (e.g., SOL, USDC). It tracks liquidity, collateral, interest rates, fees, and configuration parameters.

## Reserve

<ResponseField name="version" type="u64">
  Program version number
</ResponseField>

<ResponseField name="last_update" type="LastUpdate">
  Last update tracking slot, staleness, and price status flags
  
  <Expandable title="LastUpdate fields">
    <ResponseField name="slot" type="u64">
      Solana slot when the reserve was last updated
    </ResponseField>
    
    <ResponseField name="stale" type="u8">
      Boolean flag indicating if the reserve data is stale
    </ResponseField>
    
    <ResponseField name="price_status" type="u8">
      Bitflags for price validation status (loaded, age checked, TWAP checked, etc.)
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="lending_market" type="Pubkey">
  Parent lending market account this reserve belongs to
</ResponseField>

<ResponseField name="farm_collateral" type="Pubkey">
  Optional farm account for collateral token staking rewards
</ResponseField>

<ResponseField name="farm_debt" type="Pubkey">
  Optional farm account for debt token staking rewards
</ResponseField>

<ResponseField name="liquidity" type="ReserveLiquidity">
  Liquidity state tracking available and borrowed amounts
  
  <Expandable title="ReserveLiquidity fields">
    <ResponseField name="mint_pubkey" type="Pubkey">
      SPL Token mint for the reserve liquidity
    </ResponseField>
    
    <ResponseField name="supply_vault" type="Pubkey">
      Token account holding available liquidity
    </ResponseField>
    
    <ResponseField name="fee_vault" type="Pubkey">
      Token account for protocol fees
    </ResponseField>
    
    <ResponseField name="total_available_amount" type="u64">
      Total liquidity available for borrowing (excluding borrowed amounts)
    </ResponseField>
    
    <ResponseField name="borrowed_amount_sf" type="u128">
      Total borrowed amount in scaled fraction format
    </ResponseField>
    
    <ResponseField name="market_price_sf" type="u128">
      Current market price in scaled fraction format (quote currency per token)
    </ResponseField>
    
    <ResponseField name="market_price_last_updated_ts" type="u64">
      Unix timestamp when the market price was last updated
    </ResponseField>
    
    <ResponseField name="mint_decimals" type="u64">
      Decimal precision of the liquidity mint
    </ResponseField>
    
    <ResponseField name="deposit_limit_crossed_timestamp" type="u64">
      Timestamp when deposit limit was first crossed (0 if not crossed)
    </ResponseField>
    
    <ResponseField name="borrow_limit_crossed_timestamp" type="u64">
      Timestamp when borrow limit was first crossed (0 if not crossed)
    </ResponseField>
    
    <ResponseField name="cumulative_borrow_rate_bsf" type="BigFractionBytes">
      Cumulative borrow rate multiplier in big fraction format
    </ResponseField>
    
    <ResponseField name="accumulated_protocol_fees_sf" type="u128">
      Accumulated protocol fees in scaled fraction format
    </ResponseField>
    
    <ResponseField name="accumulated_referrer_fees_sf" type="u128">
      Accumulated referrer fees in scaled fraction format
    </ResponseField>
    
    <ResponseField name="pending_referrer_fees_sf" type="u128">
      Pending referrer fees not yet claimed
    </ResponseField>
    
    <ResponseField name="absolute_referral_rate_sf" type="u128">
      Current absolute referral rate in scaled fraction format
    </ResponseField>
    
    <ResponseField name="token_program" type="Pubkey">
      SPL Token program (Token or Token-2022)
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="collateral" type="ReserveCollateral">
  Collateral token state (cTokens representing deposits)
  
  <Expandable title="ReserveCollateral fields">
    <ResponseField name="mint_pubkey" type="Pubkey">
      Collateral token mint (cToken mint)
    </ResponseField>
    
    <ResponseField name="mint_total_supply" type="u64">
      Total supply of collateral tokens minted
    </ResponseField>
    
    <ResponseField name="supply_vault" type="Pubkey">
      Vault holding collateral tokens
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="config" type="ReserveConfig">
  Reserve configuration parameters
  
  <Expandable title="ReserveConfig fields">
    <ResponseField name="status" type="u8">
      Reserve status: Active (0), Obsolete (1), or Hidden (2)
    </ResponseField>
    
    <ResponseField name="host_fixed_interest_rate_bps" type="u16">
      Fixed interest rate charged to host in basis points
    </ResponseField>
    
    <ResponseField name="min_deleveraging_bonus_bps" type="u16">
      Minimum bonus for deleveraging operations in basis points
    </ResponseField>
    
    <ResponseField name="block_ctoken_usage" type="u8">
      Boolean flag blocking collateral token usage when set
    </ResponseField>
    
    <ResponseField name="protocol_order_execution_fee_pct" type="u8">
      Protocol fee percentage for order execution
    </ResponseField>
    
    <ResponseField name="protocol_take_rate_pct" type="u8">
      Percentage of interest taken by protocol
    </ResponseField>
    
    <ResponseField name="protocol_liquidation_fee_pct" type="u8">
      Protocol fee percentage on liquidations
    </ResponseField>
    
    <ResponseField name="loan_to_value_pct" type="u8">
      Maximum loan-to-value ratio percentage (e.g., 75 = 75%)
    </ResponseField>
    
    <ResponseField name="liquidation_threshold_pct" type="u8">
      LTV percentage at which position becomes liquidatable
    </ResponseField>
    
    <ResponseField name="min_liquidation_bonus_bps" type="u16">
      Minimum liquidation bonus in basis points
    </ResponseField>
    
    <ResponseField name="max_liquidation_bonus_bps" type="u16">
      Maximum liquidation bonus in basis points
    </ResponseField>
    
    <ResponseField name="bad_debt_liquidation_bonus_bps" type="u16">
      Liquidation bonus for bad debt in basis points
    </ResponseField>
    
    <ResponseField name="deleveraging_margin_call_period_secs" type="u64">
      Grace period in seconds before autodeleverage can execute
    </ResponseField>
    
    <ResponseField name="deleveraging_threshold_decrease_bps_per_day" type="u64">
      Rate at which deleveraging threshold decreases per day in basis points
    </ResponseField>
    
    <ResponseField name="fees" type="ReserveFees">
      Fee configuration
      
      <Expandable title="ReserveFees fields">
        <ResponseField name="origination_fee_sf" type="u64">
          Borrow origination fee in scaled fraction format
        </ResponseField>
        
        <ResponseField name="flash_loan_fee_sf" type="u64">
          Flash loan fee in scaled fraction format (u64::MAX = disabled)
        </ResponseField>
      </Expandable>
    </ResponseField>
    
    <ResponseField name="borrow_rate_curve" type="BorrowRateCurve">
      Interest rate curve defining rates based on utilization
    </ResponseField>
    
    <ResponseField name="borrow_factor_pct" type="u64">
      Borrow weight multiplier percentage (100 = 1x, 200 = 2x)
    </ResponseField>
    
    <ResponseField name="deposit_limit" type="u64">
      Maximum deposit amount allowed in liquidity tokens
    </ResponseField>
    
    <ResponseField name="borrow_limit" type="u64">
      Maximum borrow amount allowed in liquidity tokens
    </ResponseField>
    
    <ResponseField name="token_info" type="TokenInfo">
      Oracle and token metadata configuration
    </ResponseField>
    
    <ResponseField name="deposit_withdrawal_cap" type="WithdrawalCaps">
      Rate limits for deposits and withdrawals
    </ResponseField>
    
    <ResponseField name="debt_withdrawal_cap" type="WithdrawalCaps">
      Rate limits for debt operations
    </ResponseField>
    
    <ResponseField name="elevation_groups" type="[u8; 20]">
      Array of elevation group IDs this reserve belongs to
    </ResponseField>
    
    <ResponseField name="disable_usage_as_coll_outside_emode" type="u8">
      Boolean flag preventing use as collateral outside elevation mode
    </ResponseField>
    
    <ResponseField name="utilization_limit_block_borrowing_above_pct" type="u8">
      Utilization percentage above which new borrows are blocked
    </ResponseField>
    
    <ResponseField name="autodeleverage_enabled" type="u8">
      Boolean flag enabling autodeleverage for this reserve
    </ResponseField>
    
    <ResponseField name="proposer_authority_locked" type="u8">
      Boolean flag locking proposer authority changes
    </ResponseField>
    
    <ResponseField name="borrow_limit_outside_elevation_group" type="u64">
      Borrow limit for borrows outside any elevation group
    </ResponseField>
    
    <ResponseField name="borrow_limit_against_this_collateral_in_elevation_group" type="[u64; 32]">
      Per-elevation-group borrow limits when using this as collateral
    </ResponseField>
    
    <ResponseField name="deleveraging_bonus_increase_bps_per_day" type="u64">
      Rate at which deleveraging bonus increases per day in basis points
    </ResponseField>
    
    <ResponseField name="debt_maturity_timestamp" type="u64">
      Unix timestamp when debt matures (0 = no maturity)
    </ResponseField>
    
    <ResponseField name="debt_term_seconds" type="u64">
      Term length in seconds for debt (0 = no term limit)
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="borrowed_amount_outside_elevation_group" type="u64">
  Total borrowed against this reserve outside elevation groups
</ResponseField>

<ResponseField name="borrowed_amounts_against_this_reserve_in_elevation_groups" type="[u64; 32]">
  Per-elevation-group borrowed amounts against this reserve
</ResponseField>

<ResponseField name="withdraw_queue" type="WithdrawQueue">
  Withdrawal queue state for managing delayed withdrawals
  
  <Expandable title="WithdrawQueue fields">
    <ResponseField name="queued_collateral_amount" type="u64">
      Total collateral amount in the withdrawal queue
    </ResponseField>
    
    <ResponseField name="next_issued_ticket_sequence_number" type="u64">
      Next sequence number to issue for new withdrawal tickets
    </ResponseField>
    
    <ResponseField name="next_withdrawable_ticket_sequence_number" type="u64">
      Next sequence number that can be withdrawn
    </ResponseField>
  </Expandable>
</ResponseField>

## Key Concepts

### Utilization Rate
The utilization rate is calculated as: `borrowed_amount / total_supply`

### Collateral Exchange Rate
The exchange rate between collateral tokens (cTokens) and liquidity tokens changes over time as interest accrues.

### Interest Accrual
Interest is compounded continuously based on the borrow rate curve and current utilization.

## Account Size

The `Reserve` account has a fixed size defined by `RESERVE_SIZE` constant (approximately 3KB).

## Related Types

- **BorrowRateCurve**: Defines interest rates at different utilization points
- **TokenInfo**: Oracle configuration and price feed settings
- **WithdrawalCaps**: Time-based rate limiting configuration
